apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-storage
  namespace: nginx
  labels:
    app: local-storage

spec:
  storageClassName: local-storage

  accessModes:
  - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi