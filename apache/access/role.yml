apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: apache-role-manager
  namespace: apache

rules:
- apiGroups: [""]
  resources: ["pods", "hpa", "vpa, deployment", "replicaset", "daemonset", "configmap" ]
  verbs: ["get", "watch", "list", "delete", ]

