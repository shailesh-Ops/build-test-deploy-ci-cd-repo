apiVersion: v1 
kind: PersistentVolume
metadata:
  name: local-pv
  namespace: nginx

spec: 
  accessModes:
    - ReadWriteOnce

  capacity: 
    storage: 1Gi

  persistentVolumeReclaimPolicy: Retain

  storageClassName: local-storage  
  hostPath:
    path: /mnt/data

