apiVersion: v1
kind: Service
metadata:
  name: server-app-service
  namespace: server-app 
spec:
  selector:
    app: server-app
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
  type: ClusterIP 
